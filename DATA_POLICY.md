# 数据边界

公开内容只允许：通用程序、通用方法文档、完全合成的示例、从合成页面生成的图片。

## 不进入公开仓库的材料

- 实际使用过的 HAR、响应正文、日志、报表、CSV、Excel、数据库导出。
- 生产 SQL、真实表名、内部域名、看板或数据集标识、业务规则和成本参数。
- Cookie、访问令牌、密码、私钥、个人联系方式。
- 工作环境截图、历史知识库、旧 Git 历史及压缩附件。

修改真实材料中的名字或数字不构成合成数据。本仓库的示例从代码常量生成，不读取、抽样、缩放或扰动真实业务记录。

## 本地分析与公开发布的区别

解析器可以在获准环境中读取本地 HAR。其输出可能含有 SQL、字段、标识、筛选值和其他业务信息，必须继续留在获准环境中。

去掉认证请求头不代表响应正文可以公开。首版不提供“自动脱敏后即可公开”的工作流。反馈问题时请重新构造最小合成输入。

## 发布检查

1. 在独立目录准备公开文件，不复制原知识库或其 Git 历史。
2. 对每个文件确认来源；示例生成器不得读取业务文件或联网。
3. 运行 `python3 scripts/check_public_files.py`，确认文件集合和 SHA-256 与审阅清单一致。
4. 人工检查变更、图片及所有待推送提交，确认只有合成内容。
5. 只提交审阅清单中的文件。真实输入放在仓库外或忽略的 `local/` 目录。

清单校验会拒绝新文件和被修改文件，但不能识别所有商业秘密。更新清单前必须重新审阅变化；不得为绕过失败而自动重建清单。

## 示例来源

| 内容 | 来源 |
| --- | --- |
| 咖啡店订单、销售额、门店、日期 | `examples/html-bi/mock-data.js` 中的固定虚构值 |
| 示例 HAR | `scripts/generate_synthetic_har.py` 从零构造 |
| README 截图 | 只截取本仓库合成 HTML 页面 |
| 测试数据 | 测试脚本现场构造的虚构配置 |

不要将真实数据作为 Issue 附件、PR 附件或提交内容。如需报告安全问题，请先提供不含敏感内容的问题描述。

## CLI 自动化材料

CLI 的认证存储、已填写的本地配置、checkout 工程、数据集结构、资源映射、备份包和发布验收记录均属于私有材料。不要放入公开技能目录。运行环境检查不应输出任意 CLI 原始响应或本机路径。官方 CLI 从官方发布渠道安装，不随本仓库分发二进制或第三方技能副本。

新增 CLI 检查测试只使用现场构造的进程结果，不连接实际观远账户。空配置模板不包含租户、目录或数据源偏好。
